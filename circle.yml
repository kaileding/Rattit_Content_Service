machine:
  node:
    version: 6.1.0
  npm:
    version: 3.10.10
  postgres:
    version: 9.6.3

dependencies:
  cache_directories:
    - 'node_modules'

test:
  override:
    - echo "I have 16 tests for this app."
    - npm run test

deployment:
  master:
    branch: [master, staging]
    commands:
      - echo "Deployment not available yet."


